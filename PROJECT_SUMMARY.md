# 项目总结

## 项目名称

DocsGPT 企业知识库 RAG 智能客服

## 项目定位

该项目面向 AI 应用 / Agent 工程岗位，基于开源 DocsGPT 完成企业售后场景的部署配置、知识库建设、中文检索优化、边界约束和评测诊断。它不是自研 RAG 框架，也不包含 DocsGPT 源码或自训练模型。

## 已完成内容

- 完成 DocsGPT 本地部署。
- 完成 Docker Desktop、WSL、Ubuntu 环境配置。
- 解决 setup.ps1 密钥生成兼容问题。
- 解决 PostgreSQL / Alembic 初始化冲突。
- 构建电商客服知识库样例。
- 创建 DocsGPT Source。
- 创建企业客服 Agent。
- 绑定知识库 Source 与默认 RAG Prompt。
- 完成问答验证，并保留 Sources 引用。
- 设计 30 条固定回归集，覆盖知识命中、来源文件、知识边界拒答与无依据扩写。
- 编写真实 API 批量运行、断点续跑、会话导出、离线评分、失败分类与多版本对比脚本。
- 补齐“换货同款缺货”和“退货纸质发票”两项知识缺口，完成 V2 的 30 条全量回归。
- 从 V2 全量结果中发现“先拒答、再按一般常识猜测客服时段”的边界泄漏，增加严格边界规则并新建 V3 Source / Agent。
- V3 在 2026-08-07 固定 30 条真实回答快照上端到端 30/30，P95 13.6 秒；该历史快照尚未应用后续的拒答零 Sources 过滤。
- 构建 RAG 评测与链路诊断台，将四版本回归、固定集指标和单条问题的回答、来源、延迟与规则判定集中展示，并明确标注为离线评测快照。
- 增加混合检索、口语归一化和回答来源过滤；2026-09-03 重新采集的 30 条真实 API 回答快照端到端 30/30、边界题零 Sources，P50 9.803 秒、P95 25.978 秒。
- 完成受限公网发布配置与共享 Agent 外壳，将业务问答和开发诊断拆为 `/demo`、`/ops/` 两个入口；云端实例尚待部署后验收，不能把本地预览或历史快照描述成线上效果。

## 技术链路

```text
业务文档
  -> DocsGPT Source 上传
  -> 文档解析与索引
  -> 用户问题输入
  -> FAISS 向量检索 + 中文关键词检索
  -> RRF 融合召回 Sources
  -> Prompt 注入 Sources 上下文
  -> LLM 生成回答
  -> Rule Verifier 检查条件、来源与边界
```

## 项目价值

对于实习求职而言，该项目可以证明以下能力：

- 能独立复现开源 AI 应用项目。
- 能完成本地部署和工程环境排错。
- 理解 RAG 的基本工作链路。
- 能构建面向业务场景的知识库。
- 能进行 Agent 配置和问答测试。
- 能定位“检索正确但生成错误”这类真实 RAG 问题。
- 能建设可复跑评测集、分析失败类型，并以回归结果驱动知识库迭代。
- 能识别编码、流式 thought 事件污染等采集层问题，避免将链路异常混入模型质量指标。
- 能把业务问答、配置管理和评测诊断分离成不同用户路径，并为受限公网演示准备安全配置与部署门禁。

## 面试讲述建议

可以按以下顺序讲：

1. 我没有把开源框架包装成自研，而是基于 DocsGPT 完成售后场景部署、配置、排障和评测增强。
2. 我把场景设定为电商售后客服，自己构造了售后政策、会员权益和 FAQ 文档。
3. 我完成了 Source 上传、Agent 创建和问答测试。
4. 测试时遇到过一个典型问题：Sources 已经召回正确片段，但模型仍回答未找到。
5. 我通过接口和页面结果对比，判断问题不在知识库，而在 Prompt 上下文注入，最后切回默认 RAG Prompt 修复。
6. 我没有只展示单条成功问答，而是构建了 30 条固定回归集和真实 API 批量运行工具。FAQ 精简暴露出两条政策缺失，补齐后的 V2 全量回归又发现一次知识边界无依据扩写；V3 加强边界规则后再次跑满 30 条，固定集端到端通过率由 96.7% 提升到 100%。
7. 我会强调 100% 仅属于这 30 条小规模固定回归集，不能代表生产准确率；项目价值在于建立了“失败发现—分类—修复—全量回归”的闭环。

## 简历写法

```text
DocsGPT 企业知识库 RAG 智能客服
- 基于开源 DocsGPT 完成本地部署与电商售后场景化配置，搭建知识库 Source、Classic Agent 和基于 Sources 的可追溯问答链路；明确区分个人改造与上游框架能力。
- 定位“检索已召回但生成未采纳上下文”的 Prompt 配置问题，确认自定义模板缺少文档上下文注入，切回默认 RAG Prompt 后恢复回答。
- 构建 30 条固定回归集与真实 API 批量运行、断点续跑、离线评分和失败分类工具，覆盖知识条件、来源文件、边界拒答、无依据扩写与延迟指标。
- 针对知识缺失、中文口语召回和边界泄漏迭代知识库、混合检索与来源过滤；2026-09-03 的 30 条真实回答固定快照端到端 30/30、边界题零 Sources（仅代表该固定集）。
```

## 当前完成度

当前已完成本地项目主闭环、GitHub 发布内容和可复跑评测证据；公网实例仍需在完成 HTTPS、访问保护和真实 API 冒烟后验收。

如果继续提升，可以补充：

- 扩充业务文档、对抗问题和多轮问答难例
- 对同一版本执行多轮评测，分析模型随机性与置信区间
- 增加人工语义抽检和检索片段级证据一致性评测
- 完成公网部署后补充受保护的体验链接与真实部署冒烟记录
