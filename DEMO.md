# DocsGPT RAG 智能客服面试演示手册

## 1. 项目边界

本仓库保存业务知识库、DocsGPT 配置与排障方法、真实 API 回答、离线评测脚本、云端 Demo 外壳和结果；DocsGPT 官方源码与容器镜像不重复提交。演示时应明确这是基于开源 DocsGPT 的部署、场景化配置、问题修复与评测增强项目。

## 2. 公网演示入口

- 业务问答：`http://124.221.243.125:5173/demo`
- 评测诊断：`http://124.221.243.125:5173/ops/`

公网问答页隐藏管理侧栏，只保留业务身份、知识库连接状态、推荐问题、真实回答和 Sources；Source 与 Agent 配置证据见仓库截图。

## 3. 本地启动与检查

先启动 Docker Desktop，再在本仓库根目录运行：

```powershell
.\start-demo.ps1 -DocsGPTPath "E:\codex\DocsGPT"
.\check-demo.ps1 -DocsGPTPath "E:\codex\DocsGPT"
```

看到 `[PASS]` 表示前端、后端、Worker、PostgreSQL 和 Redis 均已运行。本地管理页面为 `http://127.0.0.1:5173`；不要在演示中展示 `.env`、API Key 或数据库密钥。

## 4. 三分钟演示流程

### 场景与问题

电商客服需要根据内部售后政策回答问题。通用模型可能凭常识扩写；仅看到检索结果，也不能证明生成阶段真正使用了文档上下文。

### 操作

1. 打开公网 `/demo`，点击推荐问题“质量问题退货时，运费由谁承担？”。
2. 展示回答中的平台承担条件、普通快递最高报销 12 元，以及 `customer_service_rag_optimized.md` 来源。
3. 手动输入边界问题：`离我最近的线下维修门店在哪？`
4. 展示知识库无门店信息时的拒答；拒答后不应继续展示低相关 Sources。
5. 点击“评测与诊断”，展示版本回归、固定集指标和单条问题诊断，说明它是评测快照而非生产实时 Trace。

### 重点讲解

- 曾出现“Sources 已正确召回但模型仍说未找到”的问题。接口与页面对比后定位为自定义 Prompt 未注入文档上下文，恢复默认 RAG 模板后修复。
- 英文向量模型对中文售后问题排序不稳定，因此增加中文关键词检索并与 FAISS 结果做 RRF 融合，同时加入知识边界约束。
- 通过 30 条固定用例记录真实回答、Sources、延迟和采集异常，再以失败分类驱动 FAQ、V2、V3 迭代。
- V3 固定集端到端通过率由 96.7% 提升至 100%；该结果只代表一次 30 条固定集回归，不代表生产准确率。

### 面试收口

项目贡献不是从零实现 DocsGPT，而是把开源 RAG 系统推进到“可部署、可定位、可评测、可对比、可演示”：业务用户使用简洁问答页，开发者通过独立诊断台检查检索、来源、规则和版本回归。

## 5. 演示前一分钟清单

- `/demo` 与 `/ops/` 均可打开。
- 推荐问题能够真正提交并返回回答与 Sources。
- 边界问题能够拒答，且不展示无关来源。
- 诊断台明确标注固定集和离线评测口径。
- 不展示任何密钥或本机私有配置。
